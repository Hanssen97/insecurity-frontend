import feedback from '../common/reducers/feedback';
import {reducer as session} from '../features/session';
import {reducer as content} from '../features/content';
import {reducer as user} from '../features/user';

export {
  feedback,
  session,
  content,
  user
}
