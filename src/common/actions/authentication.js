import {actions} from '../../features/session';

// export const authenticateUser = (level, store, history) => {
//   let token       = localStorage.getItem("token");
//   let {user}      = store.getState().session;

//   if (token && !user) {
//     store.dispatch(actions.getUser());
//   }

//   return true;
// }
